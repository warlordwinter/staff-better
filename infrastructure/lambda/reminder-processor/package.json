{
  "name": "reminder-processor-lambda",
  "version": "1.0.0",
  "description": "Lambda function to process reminders and send SMS via Twilio",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "node deploy-lambda.js"
  },
  "dependencies": {
    "twilio": "^5.8.0",
    "@supabase/supabase-js": "^2.53.0"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  },
  "engines": {
    "node": ">=20.x"
  }
}

