{
    "name": "reminder-worker-lambda",
    "version": "1.0.0",
    "description": "Lambda worker that sends scheduled reminder messages via Twilio",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "twilio": "^5.8.0"
    },
    "engines": {
        "node": ">=20.x"
    }
}