{
    "name": "send-worker-lambda",
    "version": "1.0.0",
    "description": "Lambda worker that sends immediate messages via Twilio",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "amqplib": "^0.10.9",
        "aws-sdk": "^2.1692.0",
        "twilio": "^5.8.0"
    },
    "engines": {
        "node": ">=20.x"
    }
}
